<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/default_menu_audit"
        android:icon="@mipmap/ic_audit"
        app:showAsAction="ifRoom"
        android:title="审核"/>
    <item android:id="@+id/default_menu_add"
        android:icon="@mipmap/ic_add_article"
        app:showAsAction="ifRoom"
        android:title="添加"/>
    <item android:id="@+id/default_menu_video"
        android:icon="@mipmap/ic_video"
        app:showAsAction="ifRoom"
        android:title="视频"/>
</menu>